-- DeutschOS migration baseline.
--
-- This intentionally creates no application tables. Every future table must be
-- added through a reviewed migration with explicit ownership, constraints,
-- indexes, grants, Row Level Security, and policy tests in the same change.

begin;

commit;
