# Curriculum module

This module will consume the canonical A1 curriculum tables through server-side
application use cases. It must not place curriculum content in UI components or
duplicate day-specific components. The authoritative 42-day import belongs in a
versioned curriculum release and its provenance records.

This foundation intentionally contains no lesson UI, learner state, review/SRS,
attempt, error, or assessment logic.
